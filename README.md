# Ort
Repositório dedicado ao web app da [automatizacao-ortese-3d](https://github.com/juliaapolonio/automatizacao-ortese-3d)

Para executar o script, clone o repositório, execute o arquivo `install.sh` e depois execute o comando `flask run` no terminal

### Por enquanto o script só funciona com imagens no formato png


# Ort
Repository dedicated to the web app of [automatizacao-ortese-3d's](https://github.com/juliaapolonio/automatizacao-ortese-3d) web app

To run, clone this repository, run `install.sh` and then run `flask run` on terminal

### For now, the app runs only png images
