# ort-back
Repositório dedicado ao backend do web app da automatização de órtese 3d

Para executar o script, clone o repositório, execute o arquivo `install.sh` e depois execute o comando `flask run` no terminal

### OBS: POR ENQUANTO, O APP SÓ SUPORTA IMAGENS NO FORMATO PNG
